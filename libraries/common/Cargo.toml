[package]
edition = "2021"
name = "common"
version = "0.1.0"

[dependencies]
scrypto = {workspace = true}
anyhow = {workspace = true}
paste = {workspace = true}

generate_config = {workspace = true}
safe_arith = {workspace = true}